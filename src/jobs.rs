use crate::company_house_response_types::ShareholderList;

pub fn get_sharholders(company_number: &String) -> ShareholderList {
    todo!()
}

pub fn get_shareholders_to_depth(company_number: &String, depth: i32) {

    // get all shareholders of company_number

    // for each of those shareholders

    // if company, get all shareholders to depth (depth - 1)
}
